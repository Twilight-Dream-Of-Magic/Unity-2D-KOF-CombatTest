namespace FightingGame.Combat {
    public enum CommandToken {
        None,
        Light,
        Heavy,
        Throw,
        Up,
        Down,
        Forward,
        Back,
        Neutral
    }
    public enum CommandChannel {
        Normal,
        Combo
    }
}